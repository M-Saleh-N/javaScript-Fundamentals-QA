//What is the difference between logical operators && and ||?


// && returns the first operand if it is false, otherwise it returns the second operand
// || returns the first operand if it is true, otherwise it returns the second operand

console.log(false && true); // Output: false
console.log(false || true); // Output: true


